{
  "name": "xberg-io/html-to-markdown",
  "description": "High-performance HTML to Markdown converter",
  "license": "MIT",
  "type": "php-ext",
  "require": {
    "php": ">=8.2"
  },
  "require-dev": {
    "phpunit/phpunit": "^13.1"
  },
  "autoload": {
    "psr-4": {
      "HtmlToMarkdown\\": "packages/php/src/"
    }
  },
  "scripts": {
    "format": "poly fmt --fix",
    "format:check": "poly fmt --check",
    "test": "php vendor/bin/phpunit",
    "lint": "poly lint",
    "lint:fix": "poly lint --fix && poly fmt --fix"
  },
  "php-ext": {
    "extension-name": "html_to_markdown",
    "support-zts": true,
    "support-nts": true,
    "download-url-method": "pre-packaged-binary"
  },
  "keywords": [
    "converter",
    "html",
    "markdown"
  ],
  "extra": {
    "pie": {
      "binary": {
        "url-template": "https://github.com/xberg-io/html-to-markdown/releases/download/{Version}/php_html_to_markdown-{Version}_php{PhpVersion}-{Arch}-{OSLower}-{Libc}-{TSMode}.tgz"
      }
    }
  }
}

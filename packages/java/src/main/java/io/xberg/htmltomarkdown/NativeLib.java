// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1521790bb224632b83d9a371dd7afd652e0c470ba5d147a8e3ef36e29f8ec3c9
// alef:handle-abi:1
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg.htmltomarkdown;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.Linker;
import java.lang.foreign.SymbolLookup;
import java.lang.foreign.ValueLayout;
import java.lang.invoke.MethodHandle;
import java.io.File;
import java.net.URL;
import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.nio.file.StandardCopyOption;
import java.util.ArrayList;
import java.util.Enumeration;
import java.util.List;
import java.util.jar.JarEntry;
import java.util.jar.JarFile;

@SuppressWarnings({"PMD.AvoidCatchingGenericException", "PMD"})
final class NativeLib {
    private static final System.Logger LOGGER = System.getLogger(NativeLib.class.getName());
    private static final Linker LINKER = Linker.nativeLinker();
    private static SymbolLookup LIB;
    private static final String NATIVES_RESOURCE_ROOT = "/natives";
    private static final String LIBRARY_PATH_PROPERTY = "html_to_markdown_ffi.library.path";
    private static final String LIBRARY_DIRECTORY_PROPERTY = "html_to_markdown_ffi.library.dir";
    private static final String SKIP_BUNDLED_PROPERTY = "html_to_markdown_ffi.skipBundled";
    private static final String LIBRARY_PATH_ENV = "HTML_TO_MARKDOWN_FFI_LIB_PATH";
    private static final String LIBRARY_DIRECTORY_ENV = "HTML_TO_MARKDOWN_FFI_LIB_DIR";
    private static final String SKIP_BUNDLED_ENV = "HTML_TO_MARKDOWN_FFI_SKIP_BUNDLED";
    private static final String[] REQUIRED_SYMBOLS = {
        "htm_conversion_options_free",
        "htm_conversion_options_from_json",
        "htm_conversion_options_update_free",
        "htm_conversion_options_update_from_json",
        "htm_conversion_result_free",
        "htm_conversion_result_from_json",
        "htm_conversion_result_to_json",
        "htm_convert",
        "htm_document_metadata_free",
        "htm_document_metadata_from_json",
        "htm_document_node_free",
        "htm_document_node_from_json",
        "htm_document_structure_free",
        "htm_document_structure_from_json",
        "htm_free_string",
        "htm_grid_cell_free",
        "htm_grid_cell_from_json",
        "htm_header_metadata_free",
        "htm_header_metadata_from_json",
        "htm_html_metadata_free",
        "htm_html_metadata_from_json",
        "htm_image_dimensions_free",
        "htm_image_dimensions_from_json",
        "htm_image_metadata_free",
        "htm_image_metadata_from_json",
        "htm_last_error_code",
        "htm_last_error_context",
        "htm_link_metadata_free",
        "htm_link_metadata_from_json",
        "htm_metadata_entry_free",
        "htm_metadata_entry_from_json",
        "htm_preprocessing_options_free",
        "htm_preprocessing_options_from_json",
        "htm_preprocessing_options_update_free",
        "htm_preprocessing_options_update_from_json",
        "htm_processing_warning_free",
        "htm_processing_warning_from_json",
        "htm_structured_data_free",
        "htm_structured_data_from_json",
        "htm_table_data_free",
        "htm_table_data_from_json",
        "htm_table_grid_free",
        "htm_table_grid_from_json",
        "htm_text_annotation_free",
        "htm_text_annotation_from_json",
        "htm_visitor_handle_free"
    };
    private static final Object NATIVE_EXTRACT_LOCK = new Object();
    private static String cachedExtractKey;
    private static Path cachedExtractDir;

    static {
        Path loadedLibraryPath = loadNativeLibrary();
        try {
            Arena arena = Arena.ofShared();
            // Try the loaded library path (for System.load() with absolute path)
            if (loadedLibraryPath != null) {
                try {
                    LIB = SymbolLookup.libraryLookup(loadedLibraryPath, arena);
                } catch (RuntimeException | Error inner) {
                    // If Path variant fails, fallback to defaultLookup
                    LIB = LINKER.defaultLookup();
                }
            } else {
                LIB = LINKER.defaultLookup();
            }
            validateRequiredSymbols(loadedLibraryPath);
        } catch (RuntimeException | Error e) {
            ExceptionInInitializerError error = new ExceptionInInitializerError(
                "Failed to initialize library symbols: " + e.getMessage());
            error.initCause(e);
            throw error;
        }
    }

    private static Path loadNativeLibrary() {
        Path loadedLibraryPath = null;
        String osName = System.getProperty("os.name", "").toLowerCase(java.util.Locale.ROOT);
        String osArch = System.getProperty("os.arch", "").toLowerCase(java.util.Locale.ROOT);

        String libName;
        String libExt;
        if (osName.contains("mac") || osName.contains("darwin")) {
            libName = "libhtml_to_markdown_ffi";
            libExt = ".dylib";
        } else if (osName.contains("win")) {
            libName = "html_to_markdown_ffi";
            libExt = ".dll";
        } else {
            libName = "libhtml_to_markdown_ffi";
            libExt = ".so";
        }

        String nativesRid = resolveNativesRid(osName, osArch);
        String nativesDir = NATIVES_RESOURCE_ROOT + "/" + nativesRid;

        String explicitPath = firstNonBlank(System.getProperty(LIBRARY_PATH_PROPERTY), System.getenv(LIBRARY_PATH_ENV));
        if (explicitPath != null) {
            return loadAbsolutePath(Paths.get(explicitPath));
        }

        String explicitDirectory = firstNonBlank(
                System.getProperty(LIBRARY_DIRECTORY_PROPERTY),
                System.getenv(LIBRARY_DIRECTORY_ENV));
        if (explicitDirectory != null) {
            return loadAbsolutePath(Paths.get(explicitDirectory, libName + libExt));
        }

        String skipBundledValue = firstNonBlank(
                System.getProperty(SKIP_BUNDLED_PROPERTY),
                System.getenv(SKIP_BUNDLED_ENV));
        if (!Boolean.parseBoolean(skipBundledValue)) {
            Path extracted = tryExtractAndLoadFromResources(nativesDir, libName, libExt);
            if (extracted != null) {
                return extracted;
            }
        }

        try {
            System.loadLibrary("html_to_markdown_ffi");
            // Find the full path by searching java.library.path
            Path libPath = findLoadedLibraryPath(libName, libExt);
            if (libPath != null) {
                loadedLibraryPath = libPath;
            }
        } catch (UnsatisfiedLinkError e) {
            String msg = "Failed to load html_to_markdown_ffi native library. Expected resource: " + nativesDir + "/" + libName
                    + libExt + " (RID: " + nativesRid + "). "
                    + "Ensure the library is bundled in the JAR under natives/{os-arch}/, "
                    + "place it on java.library.path, set -D" + LIBRARY_PATH_PROPERTY
                    + "=/absolute/path, or set " + LIBRARY_PATH_ENV + ".";
            UnsatisfiedLinkError out = new UnsatisfiedLinkError(msg + " Original error: " + e.getMessage());
            out.initCause(e);
            throw out;
        }
        return loadedLibraryPath;
    }

    private static String firstNonBlank(String preferred, String fallback) {
        if (preferred != null && !preferred.isBlank()) {
            return preferred;
        }
        if (fallback != null && !fallback.isBlank()) {
            return fallback;
        }
        return null;
    }

    private static Path loadAbsolutePath(Path configuredPath) {
        Path absolutePath = configuredPath.toAbsolutePath().normalize();
        if (!Files.isRegularFile(absolutePath)) {
            throw new UnsatisfiedLinkError("Configured native library does not exist: " + absolutePath);
        }
        System.load(absolutePath.toString());
        return absolutePath;
    }

    private static void validateRequiredSymbols(Path loadedLibraryPath) {
        List<String> missing = new ArrayList<>();
        for (String symbol : REQUIRED_SYMBOLS) {
            if (LIB.find(symbol).isEmpty() && LIB.find("_" + symbol).isEmpty()) {
                missing.add(symbol);
            }
        }
        if (missing.isEmpty()) {
            return;
        }

        int exportedCount = REQUIRED_SYMBOLS.length - missing.size();
        String loadedFrom = loadedLibraryPath == null
                ? "System.loadLibrary(html_to_markdown_ffi) / java.library.path"
                : loadedLibraryPath.toString();
        throw new UnsatisfiedLinkError(
                "The loaded html_to_markdown_ffi native library is stale relative to this binding: it exports "
                        + exportedCount + " of " + REQUIRED_SYMBOLS.length + " required symbols. Missing: "
                        + String.join(", ", missing) + ". Loaded from: " + loadedFrom
                        + ". Rebuild the native library, point this binding at a current one with -D"
                        + LIBRARY_PATH_PROPERTY + "=/absolute/path, or bypass bundled resources with -D"
                        + SKIP_BUNDLED_PROPERTY + "=true.");
    }

    private static Path tryExtractAndLoadFromResources(String nativesDir, String libName, String libExt) {
        String resourcePath = nativesDir + "/" + libName + libExt;
        URL resource = NativeLib.class.getResource(resourcePath);
        if (resource == null) {
            return null;
        }

        try {
            Path tempDir = extractOrReuseNativeDirectory(nativesDir);
            Path libPath = tempDir.resolve(libName + libExt);
            if (!Files.exists(libPath)) {
                throw new UnsatisfiedLinkError("Missing extracted native library: " + libPath);
            }
            Path absPath = libPath.toAbsolutePath();
            System.load(absPath.toString());
            return absPath;
        } catch (Exception | Error e) {
            LOGGER.log(System.Logger.Level.WARNING, "Failed to extract and load native library from resources", e);
            return null;
        }
    }

    private static Path extractOrReuseNativeDirectory(String nativesDir) throws Exception {
        URL location = NativeLib.class.getProtectionDomain().getCodeSource().getLocation();
        if (location == null) {
            throw new IllegalStateException("Missing code source location for html_to_markdown_ffi JAR");
        }

        Path codePath = Path.of(location.toURI());
        String key = codePath.toAbsolutePath() + "::" + nativesDir;

        synchronized (NATIVE_EXTRACT_LOCK) {
            if (cachedExtractDir != null && key.equals(cachedExtractKey)) {
                return cachedExtractDir;
            }
            Path tempDir = Files.createTempDirectory("html_to_markdown_ffi_native");
            tempDir.toFile().deleteOnExit();
            List<Path> extracted = extractNativeDirectory(codePath, nativesDir, tempDir);
            if (extracted.isEmpty()) {
                throw new IllegalStateException("No native files extracted from resources dir: " + nativesDir);
            }
            cachedExtractKey = key;
            cachedExtractDir = tempDir;
            return tempDir;
        }
    }

    private static List<Path> extractNativeDirectory(Path codePath, String nativesDir, Path destDir) throws Exception {
        if (!Files.exists(destDir) || !Files.isDirectory(destDir)) {
            throw new IllegalArgumentException("Destination directory does not exist: " + destDir);
        }

        String prefix = nativesDir.startsWith("/") ? nativesDir.substring(1) : nativesDir;
        if (!prefix.endsWith("/")) {
            prefix = prefix + "/";
        }

        if (Files.isDirectory(codePath)) {
            Path nativesPath = codePath.resolve(prefix);
            if (!Files.exists(nativesPath) || !Files.isDirectory(nativesPath)) {
                return List.of();
            }
            return copyDirectory(nativesPath, destDir);
        }

        List<Path> extracted = new ArrayList<>();
        try (JarFile jar = new JarFile(codePath.toFile())) {
            Enumeration<JarEntry> entries = jar.entries();
            while (entries.hasMoreElements()) {
                JarEntry entry = entries.nextElement();
                String name = entry.getName();
                if (!name.startsWith(prefix) || entry.isDirectory()) {
                    continue;
                }
                String relative = name.substring(prefix.length());
                Path out = safeResolve(destDir, relative);
                Files.createDirectories(out.getParent());
                try (var in = jar.getInputStream(entry)) {
                    Files.copy(in, out, StandardCopyOption.REPLACE_EXISTING);
                }
                out.toFile().deleteOnExit();
                extracted.add(out);
            }
        }
        return extracted;
    }

    private static List<Path> copyDirectory(Path srcDir, Path destDir) throws Exception {
        List<Path> copied = new ArrayList<>();
        try (var paths = Files.walk(srcDir)) {
            for (Path src : (Iterable<Path>) paths::iterator) {
                if (Files.isDirectory(src)) {
                    continue;
                }
                Path relative = srcDir.relativize(src);
                Path out = safeResolve(destDir, relative.toString());
                Files.createDirectories(out.getParent());
                Files.copy(src, out, StandardCopyOption.REPLACE_EXISTING);
                out.toFile().deleteOnExit();
                copied.add(out);
            }
        }
        return copied;
    }

    private static Path safeResolve(Path destDir, String relative) throws Exception {
        Path normalizedDest = destDir.toAbsolutePath().normalize();
        Path out = normalizedDest.resolve(relative).normalize();
        if (!out.startsWith(normalizedDest)) {
            throw new SecurityException("Blocked extracting native file outside destination directory: " + relative);
        }
        return out;
    }

    private static String resolveNativesRid(String osName, String osArch) {
        // Classifier names match go_java_platform(): `linux-aarch64`, `macos-arm64`,
        // `windows-x86_64`, etc. macOS distinguishes arm64 (ARM-based), while other
        // platforms use aarch64. Align with src/publish/platform.rs::go_java_platform().
        boolean isMac = osName.contains("mac") || osName.contains("darwin");
        boolean isWindows = osName.contains("win");

        String arch;
        if (osArch.contains("aarch64") || osArch.contains("arm64")) {
            // macOS uses "arm64" for aarch64; Linux uses "aarch64"
            arch = isMac ? "arm64" : "aarch64";
        } else if (osArch.contains("x86_64") || osArch.contains("amd64")) {
            arch = "x86_64";
        } else {
            arch = osArch.replaceAll("[^a-z0-9_]+", "");
        }

        String os;
        if (isMac) {
            os = "macos";
        } else if (isWindows) {
            os = "windows";
        } else {
            os = "linux";
        }

        return os + "-" + arch;
    }

    private static Path findLoadedLibraryPath(String fullLibName, String libExt) {
        // Search java.library.path for the library file
        String javaLibPath = System.getProperty("java.library.path");
        if (javaLibPath != null) {
            for (String path : javaLibPath.split(File.pathSeparator)) {
                Path libPath = Paths.get(path, fullLibName + libExt);
                if (Files.exists(libPath)) {
                    try {
                        return libPath.toRealPath();
                    } catch (java.io.IOException e) {
                        return libPath.toAbsolutePath();
                    }
                }
            }
        }
        // Library not found in java.library.path
        return null;
    }


    static final MethodHandle HTM_CONVERT = LINKER.downcallHandle(
        LIB.find("htm_convert")
            .or(() -> LIB.find("_htm_convert"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_convert"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_convert"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "htm_convert" +
                " (tried: htm_convert, _htm_convert). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_FREE_STRING = LINKER.downcallHandle(
        LIB.find("htm_free_string")
            .or(() -> LIB.find("_htm_free_string"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_free_string"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_free_string"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_LAST_ERROR_CODE = LINKER.downcallHandle(
        LIB.find("htm_last_error_code").orElse(null),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG)
    );

    static final MethodHandle HTM_LAST_ERROR_CONTEXT = LINKER.downcallHandle(
        LIB.find("htm_last_error_context").orElse(null),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_DOCUMENT_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_document_metadata_from_json")
            .or(() -> LIB.find("_htm_document_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_document_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_document_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_DOCUMENT_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("htm_document_metadata_free")
            .or(() -> LIB.find("_htm_document_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_document_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_document_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_HEADER_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_header_metadata_from_json")
            .or(() -> LIB.find("_htm_header_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_header_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_header_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_HEADER_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("htm_header_metadata_free")
            .or(() -> LIB.find("_htm_header_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_header_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_header_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_LINK_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_link_metadata_from_json")
            .or(() -> LIB.find("_htm_link_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_link_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_link_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_LINK_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("htm_link_metadata_free")
            .or(() -> LIB.find("_htm_link_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_link_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_link_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_IMAGE_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_image_metadata_from_json")
            .or(() -> LIB.find("_htm_image_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_image_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_image_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_IMAGE_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("htm_image_metadata_free")
            .or(() -> LIB.find("_htm_image_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_image_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_image_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_STRUCTURED_DATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_structured_data_from_json")
            .or(() -> LIB.find("_htm_structured_data_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_structured_data_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_structured_data_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_STRUCTURED_DATA_FREE = LINKER.downcallHandle(
        LIB.find("htm_structured_data_free")
            .or(() -> LIB.find("_htm_structured_data_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_structured_data_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_structured_data_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_HTML_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_html_metadata_from_json")
            .or(() -> LIB.find("_htm_html_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_html_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_html_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_HTML_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("htm_html_metadata_free")
            .or(() -> LIB.find("_htm_html_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_html_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_html_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_CONVERSION_OPTIONS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_conversion_options_from_json")
            .or(() -> LIB.find("_htm_conversion_options_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_conversion_options_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_conversion_options_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_CONVERSION_OPTIONS_FREE = LINKER.downcallHandle(
        LIB.find("htm_conversion_options_free")
            .or(() -> LIB.find("_htm_conversion_options_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_conversion_options_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_conversion_options_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_CONVERSION_OPTIONS_UPDATE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_conversion_options_update_from_json")
            .or(() -> LIB.find("_htm_conversion_options_update_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_conversion_options_update_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_conversion_options_update_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_CONVERSION_OPTIONS_UPDATE_FREE = LINKER.downcallHandle(
        LIB.find("htm_conversion_options_update_free")
            .or(() -> LIB.find("_htm_conversion_options_update_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_conversion_options_update_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_conversion_options_update_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_PREPROCESSING_OPTIONS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_preprocessing_options_from_json")
            .or(() -> LIB.find("_htm_preprocessing_options_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_preprocessing_options_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_preprocessing_options_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_PREPROCESSING_OPTIONS_FREE = LINKER.downcallHandle(
        LIB.find("htm_preprocessing_options_free")
            .or(() -> LIB.find("_htm_preprocessing_options_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_preprocessing_options_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_preprocessing_options_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_PREPROCESSING_OPTIONS_UPDATE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_preprocessing_options_update_from_json")
            .or(() -> LIB.find("_htm_preprocessing_options_update_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_preprocessing_options_update_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_preprocessing_options_update_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_PREPROCESSING_OPTIONS_UPDATE_FREE = LINKER.downcallHandle(
        LIB.find("htm_preprocessing_options_update_free")
            .or(() -> LIB.find("_htm_preprocessing_options_update_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_preprocessing_options_update_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_preprocessing_options_update_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_IMAGE_DIMENSIONS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_image_dimensions_from_json")
            .or(() -> LIB.find("_htm_image_dimensions_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_image_dimensions_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_image_dimensions_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_IMAGE_DIMENSIONS_FREE = LINKER.downcallHandle(
        LIB.find("htm_image_dimensions_free")
            .or(() -> LIB.find("_htm_image_dimensions_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_image_dimensions_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_image_dimensions_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_DOCUMENT_STRUCTURE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_document_structure_from_json")
            .or(() -> LIB.find("_htm_document_structure_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_document_structure_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_document_structure_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_DOCUMENT_STRUCTURE_FREE = LINKER.downcallHandle(
        LIB.find("htm_document_structure_free")
            .or(() -> LIB.find("_htm_document_structure_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_document_structure_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_document_structure_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_DOCUMENT_NODE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_document_node_from_json")
            .or(() -> LIB.find("_htm_document_node_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_document_node_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_document_node_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_DOCUMENT_NODE_FREE = LINKER.downcallHandle(
        LIB.find("htm_document_node_free")
            .or(() -> LIB.find("_htm_document_node_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_document_node_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_document_node_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_TEXT_ANNOTATION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_text_annotation_from_json")
            .or(() -> LIB.find("_htm_text_annotation_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_text_annotation_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_text_annotation_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_TEXT_ANNOTATION_FREE = LINKER.downcallHandle(
        LIB.find("htm_text_annotation_free")
            .or(() -> LIB.find("_htm_text_annotation_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_text_annotation_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_text_annotation_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_METADATA_ENTRY_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_metadata_entry_from_json")
            .or(() -> LIB.find("_htm_metadata_entry_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_metadata_entry_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_metadata_entry_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_METADATA_ENTRY_FREE = LINKER.downcallHandle(
        LIB.find("htm_metadata_entry_free")
            .or(() -> LIB.find("_htm_metadata_entry_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_metadata_entry_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_metadata_entry_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_CONVERSION_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_conversion_result_from_json")
            .or(() -> LIB.find("_htm_conversion_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_conversion_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_conversion_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_CONVERSION_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("htm_conversion_result_free")
            .or(() -> LIB.find("_htm_conversion_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_conversion_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_conversion_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_TABLE_GRID_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_table_grid_from_json")
            .or(() -> LIB.find("_htm_table_grid_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_table_grid_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_table_grid_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_TABLE_GRID_FREE = LINKER.downcallHandle(
        LIB.find("htm_table_grid_free")
            .or(() -> LIB.find("_htm_table_grid_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_table_grid_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_table_grid_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_GRID_CELL_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_grid_cell_from_json")
            .or(() -> LIB.find("_htm_grid_cell_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_grid_cell_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_grid_cell_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_GRID_CELL_FREE = LINKER.downcallHandle(
        LIB.find("htm_grid_cell_free")
            .or(() -> LIB.find("_htm_grid_cell_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_grid_cell_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_grid_cell_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_TABLE_DATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_table_data_from_json")
            .or(() -> LIB.find("_htm_table_data_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_table_data_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_table_data_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_TABLE_DATA_FREE = LINKER.downcallHandle(
        LIB.find("htm_table_data_free")
            .or(() -> LIB.find("_htm_table_data_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_table_data_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_table_data_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_PROCESSING_WARNING_FROM_JSON = LINKER.downcallHandle(
        LIB.find("htm_processing_warning_from_json")
            .or(() -> LIB.find("_htm_processing_warning_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_processing_warning_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_processing_warning_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_PROCESSING_WARNING_FREE = LINKER.downcallHandle(
        LIB.find("htm_processing_warning_free")
            .or(() -> LIB.find("_htm_processing_warning_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_processing_warning_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_processing_warning_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_VISITOR_HANDLE_FREE = LINKER.downcallHandle(
        LIB.find("htm_visitor_handle_free")
            .or(() -> LIB.find("_htm_visitor_handle_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("htm_visitor_handle_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_htm_visitor_handle_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle HTM_CONVERSION_RESULT_TO_JSON = LIB.find("htm_conversion_result_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle HTM_REGISTER_HTML_VISITOR = LIB
        .find("htm_register_html_visitor")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


}

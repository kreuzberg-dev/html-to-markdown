// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg.htmltomarkdown;

import java.lang.foreign.MemorySegment;

/**
 * Shareable, thread-safe handle to a user-provided HTML visitor implementation.
 *
 * Pass an instance wrapped in this handle to {@code ConversionOptions} to
 * customise how the HTML document is traversed and converted to Markdown.
 * The handle may be cloned and shared across threads without additional
 * synchronisation on the caller's side.
 */
@SuppressWarnings("PMD")
public class VisitorHandle implements AutoCloseable {
    private MemorySegment handle;

    VisitorHandle(MemorySegment handle) {
        this.handle = handle;
    }

    synchronized MemorySegment handle() {
        if (this.handle == null || this.handle.equals(MemorySegment.NULL)) {
            throw new IllegalStateException("VisitorHandle is closed");
        }
        return this.handle;
    }
    @Override
    @SuppressWarnings("PMD.AvoidCatchingGenericException")
    public synchronized void close() {
        if (handle == null || handle.equals(MemorySegment.NULL)) {
            return;
        }
        MemorySegment handleToFree = handle;
        handle = MemorySegment.NULL;
        try {
            NativeLib.HTM_VISITOR_HANDLE_FREE.invoke(handleToFree);
        } catch (Throwable e) {
            throw new RuntimeException("Failed to free VisitorHandle: " + e.getMessage(), e);
        }
    }
}

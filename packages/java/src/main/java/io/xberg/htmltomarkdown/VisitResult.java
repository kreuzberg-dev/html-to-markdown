// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a42d2a8295a310843fa1d8169736ab97249179654ab5715886ad8376a908d722
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg.htmltomarkdown;

import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.node.ObjectNode;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import org.jspecify.annotations.Nullable;

/**
 * Result of a visitor callback.
 *
 * Allows visitors to control the conversion flow by either proceeding
 * with default behavior, providing custom output, skipping elements,
 * preserving HTML, or signaling errors.
 */
@com.fasterxml.jackson.annotation.JsonIgnoreProperties(ignoreUnknown = true)
@JsonDeserialize(using = VisitResultDeserializer.class)
@JsonSerialize(using = VisitResultSerializer.class)
public sealed interface VisitResult {

    /**
     * Continue with default conversion behavior
     */
    record Continue() implements VisitResult {
    }

    /**
     * Replace default output with custom markdown
     *
     * The visitor takes full responsibility for the markdown output
     * of this node and its children.
     */
    record Custom(String value) implements VisitResult { }

    /**
     * Skip this element entirely (don't output anything)
     *
     * The element and all its children are ignored in the output.
     */
    record Skip() implements VisitResult {
    }

    /**
     * Preserve original HTML (don't convert to markdown)
     *
     * The element's raw HTML is included verbatim in the output.
     */
    record PreserveHtml() implements VisitResult {
    }

    /**
     * Stop conversion with an error
     *
     * The conversion process halts and returns this error message.
     */
    record Error(String value) implements VisitResult { }

    /** Returns the Custom data if this is a Custom variant, otherwise null. */
    default @Nullable String custom() {
        return this instanceof Custom variant ? variant.value() : null;
    }

    /** Returns the Error data if this is a Error variant, otherwise null. */
    default @Nullable String error() {
        return this instanceof Error variant ? variant.value() : null;
    }

}

/** Custom deserializer for sealed interface with unwrapped variants. */
class VisitResultDeserializer extends StdDeserializer<VisitResult> {
  private static final long serialVersionUID = 1L;

  VisitResultDeserializer() {
    super(VisitResult.class);
  }

  @Override
  @SuppressWarnings("PMD.CyclomaticComplexity")
  public VisitResult deserialize(final JsonParser parser, final DeserializationContext ctx)
      throws java.io.IOException {
    final ObjectNode node = parser.getCodec().readTree(parser);
    final com.fasterxml.jackson.databind.JsonNode tagNode = node.get("type");
    if (tagNode == null || tagNode.isNull()) {
      throw new com.fasterxml.jackson.databind.JsonMappingException(
          parser, "Missing discriminator field: type");
    }
    final String tagValue = tagNode.asText();
    node.remove("type");

    return switch (tagValue) {
      case "continue" -> new VisitResult.Continue();
      case "custom" -> new VisitResult.Custom(node.toString());
      case "skip" -> new VisitResult.Skip();
      case "preserve_html" -> new VisitResult.PreserveHtml();
      case "error" -> new VisitResult.Error(node.toString());
      default -> {
        throw new com.fasterxml.jackson.databind.JsonMappingException(
            parser, "Unknown VisitResult discriminator: " + tagValue);
      }
    };
  }
}

/** Custom serializer for sealed interface with unwrapped variants. Emits the discriminator tag alongside the inner record's fields (flat object). */
class VisitResultSerializer extends StdSerializer<VisitResult> {
  private static final long serialVersionUID = 1L;
  /** ObjectMapper for serializing inner variant values. */
  private static final com.fasterxml.jackson.databind.ObjectMapper MAPPER =
      new com.fasterxml.jackson.databind.ObjectMapper()
          .registerModule(new com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
          .setPropertyNamingStrategy(
              com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
          .setSerializationInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL);

  /** Creates a new VisitResultSerializer. */
  VisitResultSerializer() {
    super(VisitResult.class);
  }

  @Override
  @SuppressWarnings("PMD.CyclomaticComplexity")
  public void serialize(VisitResult value, JsonGenerator gen, SerializerProvider provider)
      throws java.io.IOException {
    String tag;
    Object inner;if (value instanceof VisitResult.Continue variantContinue) {
      tag = "continue";      inner = null;    }else if (value instanceof VisitResult.Custom variantCustom) {
      tag = "custom";      inner = variantCustom.value();    }else if (value instanceof VisitResult.Skip variantSkip) {
      tag = "skip";      inner = null;    }else if (value instanceof VisitResult.PreserveHtml variantPreserveHtml) {
      tag = "preserve_html";      inner = null;    }else if (value instanceof VisitResult.Error variantError) {
      tag = "error";      inner = variantError.value();    }    else {
      throw new com.fasterxml.jackson.databind.JsonMappingException(
          gen, "Unknown VisitResult variant: " + value.getClass().getName());
    }

    gen.writeStartObject();
    gen.writeStringField("type", tag);
    if (inner != null) {
      com.fasterxml.jackson.databind.JsonNode tree = MAPPER.valueToTree(inner);
      if (tree.isObject()) {
        java.util.Iterator<java.util.Map.Entry<String, com.fasterxml.jackson.databind.JsonNode>>
            fieldIterator = tree.fields();
        while (fieldIterator.hasNext()) {
          java.util.Map.Entry<String, com.fasterxml.jackson.databind.JsonNode> field =
              fieldIterator.next();
          gen.writeFieldName(field.getKey());
          gen.writeTree(field.getValue());
        }
      }
    }
    gen.writeEndObject();
  }
}
